[build]
dockerfilePath = "Dockerfile.railway"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 120
startCommand = "/app/scripts/entrypoint-railway.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
